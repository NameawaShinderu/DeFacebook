pragma solidity >=0.4.21 <0.6.0;

contract dfab{                                   //during any kind of truffle development call the contract name not the .sol file name, so call DFAB instead of defacebook

    string public name = "defacebook";

}